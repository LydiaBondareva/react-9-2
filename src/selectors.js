export const selectSearchValue = (state) => state.searchValue;
export const selectIsSorted = (state) => state.isSorted;
export const selectNewTodo = (state) => state.newTodo;
export const selectIsLoading = (state) => state.isLoading;
export const selectError = (state) => state.error;
export const selectTodos = (state) => state.todos;
export const selectNewTaskValue = (state) => state.newTaskValue;
