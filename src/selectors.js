export const selectSearchValue = (state) => state.allState.searchValue;
export const selectIsSorted = (state) => state.allState.isSorted;
export const selectNewTodo = (state) => state.allState.newTodo;
export const selectIsLoading = (state) => state.allState.isLoading;
export const selectError = (state) => state.allState.error;
export const selectTodos = (state) => state.todosState;
export const selectNewTaskValue = (state) => state.allState.newTaskValue;
